<h1> Tasks </h1>

<%= link_to "Add a new task", new_task_path %>

<% @tasks.each do |task| %>
<h2> <%= task.name %> </h2>
<p> <%= task.done %> </p>
<%= link_to "Go to task", task_path(task) %>
<%= link_to "Delete task", task_path(task), method: :delete %>
<% end %>
